## Next steps :
- Check if the numerical instabilities in the log-loss plot comes from the flops in metal [float32 vs float 64] (try cuda) 
- Refactor the pipeline to easily switch the optimizers

# After this :
- Implement excluded loss
- See if the probe implementation as regularizer could be useful for our outlook.